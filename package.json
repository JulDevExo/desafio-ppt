{
  "name": "desafio-ppt",
  "homepage": "https://desafio-ppt.vercel.app",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url /",
    "dev": "npm run start && npm run build",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/optimizer-terser": "^2.16.1",
    "gh-pages": "^6.1.1",
    "parcel": "^2.16.1",
    "process": "^0.11.10"
  },
  "dependencies": {
    "firebase": "^12.6.0"
  }
}
